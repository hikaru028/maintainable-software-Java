package kalah.move.playerChoice;

import kalah.player.Player;

/**
 * Represents an interface for obtaining a player's choice.
 */
public interface PlayerChoice {
    int getPlayerChoice(Player currentPlayer);
}
