package kalah.utils.printer.boardPrinter;

/**
 * This interface represents the contract for printing a game board.
 * Implementations of this interface should provide the logic to visually represent
 * the board, depending on the game's rules and desired display format.
 */
public interface BoardPrinter {
    void printBoard();
}